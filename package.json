{
  "name": "bloglist-backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "cross-env NODE_ENV=production node src",
    "dev": "cross-env NODE_ENV=development node --watch --env-file=.env --env-file=.env.local src",
    "test": "cross-env NODE_ENV=test node --test --env-file=.env --env-file=.env.test.local",
    "format": "prettier -w ."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "express": "^4.19.2",
    "express-async-errors": "^3.1.1",
    "mongoose": "^8.5.1",
    "tiny-invariant": "^1.3.3"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "prettier-plugin-organize-imports": "^4.0.0",
    "supertest": "^7.0.0"
  }
}
